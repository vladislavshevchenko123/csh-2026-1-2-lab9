<Window x:Class="lab9.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:lab9"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Grid>
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel Orientation="Horizontal" Grid.Row="0">
                <TextBlock Text="Имя:" VerticalAlignment="Center" Margin="0,0,5,0" Width="100"/>
                <TextBox Margin="0,5" Width="300" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBlock Text="{Binding NameValidationError}" VerticalAlignment="Center" Margin="10,0,5,0" Width="300" Foreground="Red"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Grid.Row="1">
                <TextBlock Text="Телефон:" VerticalAlignment="Center" Margin="0,0,5,0" Width="100"/>
                <TextBox Margin="0,5" Width="300" Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBlock Text="{Binding PhoneValidationError}" VerticalAlignment="Center" Margin="10,0,5,0" Width="300" Foreground="Red"/>
            </StackPanel>
            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10">
                <Button Content="Добавить" Width="100" Margin="0,0,10,0" Command="{Binding AddCommand}"/>
                <Button Content="Удалить" Width="100" Command="{Binding DeleteCommand}"/>
            </StackPanel>
            <!-- Список контактов -->
            <DataGrid Grid.Row="3" AutoGenerateColumns="False" IsReadOnly="True" 
                 ItemsSource="{Binding Contacts}" 
                 SelectedItem="{Binding SelectedContact}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Имя" Binding="{Binding Name}"/>
                    <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

    </Grid>
</Window>
